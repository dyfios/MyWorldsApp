# Build optimization settings to prevent hanging
GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false

# Reduce memory usage during build
NODE_OPTIONS=--max-old-space-size=4096

# Disable progress indicators that might cause issues on some systems
CI=true